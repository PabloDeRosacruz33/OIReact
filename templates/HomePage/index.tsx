import Layout from "@/components/Layout";
import Main from "./Main";

const HomePage = () => {
    return (
        <Layout>
            <Main />
        </Layout>
    );
};

export default HomePage;
